---
description: 仅读写当前项目目录，禁止访问外部路径
alwaysApply: true
---

# 工作区范围

所有文件读写操作必须限制在当前项目根目录内。

## 允许

- 读取、创建、修改、删除当前项目目录下的文件
- 在当前目录内运行命令（如构建、测试、git）

## 禁止

- 读取或写入项目目录外的文件（如 `~/.cursor/`、`/tmp/`、其他项目路径）
- 使用绝对路径访问外部目录
- 修改用户全局配置（除非用户明确要求且文件位于当前目录内）

## 例外

- 用户在本条消息或后续消息中**明确授权**访问特定外部路径时，仅可访问该路径
